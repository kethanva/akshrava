<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--
      A rear camera is not optional for this app: without it there is nothing to detect and the
      service refuses to start. Declaring required="true" keeps it off camera-less devices where
      it could only ever mislead a user who depends on it. Lint's ChromeOS check asks for
      required="false" to widen device support; that trade is wrong here, so it is suppressed
      deliberately rather than silenced by weakening the declaration.
    -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />

    <uses-permission
        android:name="android.permission.CAMERA"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Akshrava">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".AssistService"
            android:exported="false"
            android:foregroundServiceType="camera" />
        <receiver
            android:name=".StopReceiver"
            android:exported="false" />
        <receiver
            android:name=".WatchdogReceiver"
            android:exported="false" />
    </application>
</manifest>
